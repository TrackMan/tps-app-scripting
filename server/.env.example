# Server Configuration
PORT=4000

# Webhook Configuration
# Comma-separated list of allowed webhook paths (optional, leave empty to allow all)
WEBHOOK_ALLOWED_KEYS=

# Azure Table Storage for persistent webhook event storage
# Format: DefaultEndpointsProtocol=https;AccountName=<name>;AccountKey=<key>;EndpointSuffix=core.windows.net
# If not set, events will only be stored in-memory (lost on restart)
AZURE_STORAGE_CONNECTION_STRING=

# CORS Configuration (optional)
# CORS_ORIGIN=https://app-scripting-editor.trackmangolfdev.com
